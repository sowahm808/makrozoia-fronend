import { Component, inject } from '@angular/core';import { AsyncPipe } from '@angular/common';import { RouterLink } from '@angular/router';import { switchMap, of } from 'rxjs';import { AuthService } from '../../services/auth.service';import { CompanyProfileService } from '../../services/company-profile.service';
@Component({standalone:true,imports:[AsyncPipe,RouterLink],template:`<section class="page">@if(profile$ | async; as profile){<div class="dashboard-hero"><div><p class="eyebrow">Client dashboard</p><h1>Welcome, {{profile.companyName}}</h1><p>{{profile.companyDescription}}</p></div><a class="button" routerLink="/contact">Request consultation</a></div><div class="cards"><article><h3>Company summary</h3><p><strong>Industry:</strong> {{profile.industry}}</p><p><strong>Size:</strong> {{profile.companySize}}</p><p><strong>Location:</strong> {{profile.headquartersLocation}}</p></article><article><h3>Recommended next steps</h3><ol><li>Review your selected services with a Makrozoia architect.</li><li>Schedule a discovery workshop.</li><li>Confirm project scope, timeline, and delivery model.</li></ol></article><article><h3>Actions</h3><a class="button" routerLink="/company-profile">Update company profile</a></article></div><h2>Services selected</h2><div class="pill-grid">@for(service of profile.servicesInterestedIn; track service){<span>{{service}}</span>}</div>} @else {<p>Loading dashboard…</p>}</section>`})export class DashboardComponent{private auth=inject(AuthService);private profiles=inject(CompanyProfileService);profile$=this.auth.user$.pipe(switchMap(user=>user?this.profiles.getProfileByUid(user.uid):of(null)));}
